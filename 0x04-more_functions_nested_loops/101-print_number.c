#include "main.h"

/**
 * print_number - prints an integer.
 * @n: integer
 * Return: Always O
 */
void print_number(int n)
{
	int i;
	char c;

	while (i = 0; i < 10; i++)
	{
		c = m;
		if (c > 9)
		{
			_putchar('1');
			c = m % 10;
		}
		_putchar('0' + c);
	}
}
