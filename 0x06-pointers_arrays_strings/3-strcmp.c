#include <string.h>
/**
 * _strcmp - function that compares two strings
 * @s1: pointer
 * @s2: pointer
 * Return: integer to `resultat`
 */
int _strcmp(char *s1, char *s2)
{
	int resultat;

	resultat = strcmp(s1, s2);
	return (resultat);
}
